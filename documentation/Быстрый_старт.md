# Быстрый старт

Краткое руководство по установке и запуску системы **simple-SCADA**.

## 📋 Предварительные требования

- **Node.js** версии 18 или выше
- **npm** версии 9 или выше
- **COM порт** для работы с Modbus RTU устройствами (RS-485 интерфейс)
- **Git** (опционально, для клонирования репозитория)

## 🚀 Установка

### Шаг 1: Получение исходного кода

**Вариант 1: Клонирование через Git**

```bash
git clone https://github.com/IvanMochalov/simple-SCADA.git
cd simple-SCADA
```

**Вариант 2: Скачивание архива**

1. Перейдите на https://github.com/IvanMochalov/simple-SCADA
2. Нажмите кнопку **"Code"** → **"Download ZIP"**
3. Распакуйте архив в нужную директорию
4. Откройте терминал в директории проекта

### Шаг 2: Установка зависимостей

**Вариант 1: Установка из корневой директории**

```bash
npm install
```

**Вариант 2: Установка отдельно для сервера и клиента**

```bash
# Установка зависимостей сервера
cd server
npm install

# Установка зависимостей клиента
cd ../client
npm install
```

### Шаг 3: Настройка базы данных

```bash
cd server
npx prisma generate
npx prisma migrate dev --name init
```

**Результат:**

- База данных SQLite создана в `server/prisma/data.db`
- Схема базы данных применена
- Prisma Client сгенерирован

## ▶️ Запуск

### Обычный запуск (production) — только из корня

Этот вариант подходит, когда нужно запустить систему “как приложение”: сервер будет отдавать собранный интерфейс из
`client/dist`.

```bash
npm run start
```

Доступ после запуска:

- **Веб-интерфейс + API:** `http://localhost:3001`
- **WebSocket:** `ws://localhost:3001`

### Запуск в dev-режиме

#### Вариант 1: Запуск сервера и клиента отдельно

**Терминал 1 — Сервер:**

```bash
cd server
npm run dev
```

**Терминал 2 — Клиент:**

```bash
cd client
npm run dev
```

Доступ после запуска:

- **Сервер API:** `http://localhost:3001`
- **Веб-интерфейс (Vite):** `http://localhost:5173`
- **WebSocket:** `ws://localhost:3001`

#### Вариант 2: Запуск dev через корневой package.json

```bash
npm run dev
```

**Примечание:** Для `npm run dev` из корня нужен пакет `concurrently` в корневых зависимостях (он уже указан в
`simple-SCADA/package.json`).

## ⚙️ Настройка конфигурации

### Шаг 1: Создание узла связи

1. Откройте браузер на `http://localhost:5173`
2. Перейдите в раздел **"Конфигурация"**
3. Нажмите кнопку **"Добавить узел связи"**
4. Заполните форму:
    - **Название:** например, "RS-485 ТРМ212"
    - **Тип узла:** COM порт
    - **Порт:** например, "COM3" (укажите ваш COM порт)
    - **Скорость (бод):** 9600 (или другая, в зависимости от устройства)
    - **Биты данных:** 8
    - **Стоп-биты:** 1
    - **Четность:** Не используется
    - **Включен в работу:** ✓
5. Нажмите **"Сохранить"**

### Шаг 2: Добавление устройства

1. Нажмите кнопку **"+"** рядом с созданным узлом связи
2. Заполните форму:
    - **Название:** например, "ТРМ212-1"
    - **Адрес Modbus:** например, 17 (адрес вашего устройства)
    - **Время ответа (мс):** 1000 (рекомендуется)
    - **Период опроса (мс):** 1000 (рекомендуется для одного устройства)
    - **Включен в работу:** ✓
3. Нажмите **"Сохранить"**

**Важно:**

- Адрес Modbus должен соответствовать адресу, настроенному на физическом устройстве
- Для нескольких устройств на одной шине рекомендуется период опроса не менее 2000 мс

### Шаг 3: Добавление тегов

1. Нажмите кнопку **"+"** рядом с созданным устройством
2. Заполните форму:
    - **Название:** например, "Температура датчика"
    - **Адрес регистра:** например, 1 (адрес регистра в устройстве)
    - **Тип регистра:** Holding Register (или другой, в зависимости от устройства)
    - **Тип данных в устройстве:** int16 (или другой, в зависимости от устройства)
    - **Тип данных в сервере:** int32
    - **Множитель:** 0.1 (или другой, в зависимости от устройства)
    - **Тип доступа:** ReadOnly (или ReadWrite, если нужна запись)
    - **Включен в работу:** ✓
3. Нажмите **"Сохранить"**

**Важно:**

- Адрес регистра должен соответствовать адресу регистра в документации устройства
- Для float значений система автоматически читает 2 регистра (32 бита)

### Примеры конфигурации для тестированных устройств

#### ТРМ212 (ОВЕН)

**Устройство:**

- Название: ТРМ212-1
- Адрес Modbus: 17
- Время ответа: 1000 мс
- Период опроса: 1000 мс

**Тег 1: Температура датчика**

- Название: Температура датчика
- Адрес регистра: 1
- Тип регистра: Holding Register
- Тип данных в устройстве: int16
- Тип данных в сервере: int32
- Множитель: 0.1
- Тип доступа: ReadOnly

**Тег 2: Уставка**

- Название: Уставка
- Адрес регистра: 2
- Тип регистра: Holding Register
- Тип данных в устройстве: int16
- Тип данных в сервере: int32
- Множитель: 0.1
- Тип доступа: ReadWrite

#### МВ110.224-8а (ОВЕН)

**Устройство:**

- Название: МВ110-1
- Адрес Modbus: 16
- Время ответа: 1000 мс
- Период опроса: 500 мс

**Тег 1: Аналоговый вход 1**

- Название: Аналоговый вход 1
- Адрес регистра: 22
- Тип регистра: Holding Register
- Тип данных в устройстве: float
- Тип данных в сервере: float
- Множитель: 1
- Тип доступа: ReadOnly

**Тег 2: Аналоговый вход 2**

- Название: Аналоговый вход 2
- Адрес регистра: 40
- Тип регистра: Holding Register
- Тип данных в устройстве: float
- Тип данных в сервере: float
- Множитель: 0.1
- Тип доступа: ReadOnly

## 📊 Просмотр данных

### Реальное время

1. Перейдите в раздел **"Реальное время"**
2. Нажмите кнопку **"Запустить Modbus Server"**
3. Дождитесь инициализации соединений
4. Наблюдайте за обновлением значений тегов в реальном времени

**Особенности:**

- Значения обновляются автоматически через WebSocket
- Для тегов с типом доступа ReadWrite можно записывать значения
- Статусы узлов связи отображаются в реальном времени
- При ошибках соединения отображаются уведомления

### История

1. Перейдите в раздел **"История"**
2. Нажмите кнопку **"Фильтры"**
3. Настройте фильтры:
    - Выберите уровень (система/узел/устройство)
    - Выберите временной диапазон
4. Нажмите **"Загрузить историю"**
5. Просмотрите данные в таблице или на графике

**Особенности:**

- Данные группируются по времени
- Можно выбрать теги для отображения на графике
- Таблица поддерживает сортировку и пагинацию

## ⚙️ Настройка интервала архивации

1. Нажмите на иконку **"⚙️"** рядом со статусом соединения
2. Выберите интервал архивации из списка (от 5 секунд до 5 минут)
3. Нажмите **"Сохранить"**

**Рекомендации:**

- Для быстрых процессов: 10-30 секунд
- Для обычных процессов: 60 секунд (по умолчанию)
- Для медленных процессов: 300-600 секунд

## ⚠️ Важные замечания

### COM порт

- Убедитесь, что COM порт не используется другими приложениями
- На Windows может потребоваться запуск с правами администратора
- Проверьте, что порт существует в системе (Диспетчер устройств Windows)

### Параметры опроса

- **Время ответа (responseTimeout):** Рекомендуется 1000-2000 мс
- **Период опроса (pollInterval):**
    - Одно устройство: 500-1000 мс
    - Несколько устройств: рассчитывается по формуле: `(Количество устройств × responseTimeout) + 500 мс`

### Статусы

- **Подключен** (зеленый) — соединение установлено, данные поступают
- **Отключен** (серый) — соединение не установлено
- **Ошибка соединения** (красный) — ошибка подключения (COM порт занят, устройство не отвечает)

## 🐛 Устранение проблем

### Ошибка "COM порт занят"

**Причина:** COM порт используется другим приложением

**Решение:**

1. Закройте другие приложения, использующие COM порт
2. Перезапустите сервер
3. Проверьте, что MasterOPC Universal Modbus Server не запущен

### Ошибка "Не удалось открыть COM порт"

**Причина:** COM порт не существует или нет прав доступа

**Решение:**

1. Проверьте, что COM порт существует в системе
2. На Windows запустите сервер с правами администратора
3. Проверьте настройки COM порта в конфигурации

### Устройство показывает статус "Ошибка соединения"

**Причина:** Устройство не отвечает или неправильные настройки

**Решение:**

1. Проверьте физическое подключение (RS-485)
2. Проверьте, что Modbus адрес правильный
3. Проверьте настройки COM порта (скорость, биты данных, четность)
4. Используйте кнопку "Переподключить" для устройства
5. Увеличьте время ответа (responseTimeout)

### Значения тегов не обновляются

**Причина:** Modbus Manager не запущен или устройство не отвечает

**Решение:**

1. Проверьте, что Modbus Manager запущен (кнопка "Остановить Modbus Server")
2. Проверьте статус узла связи (должен быть "Подключен")
3. Проверьте, что устройство и тег включены в работу
4. Используйте кнопку "Переподключить" для устройства

### Нет исторических данных

**Причина:** Modbus Manager не был запущен или устройства не опрашивались

**Решение:**

1. Убедитесь, что Modbus Manager был запущен в течение выбранного периода
2. Проверьте, что устройства и теги были включены в работу
3. Расширьте временной диапазон поиска
4. Проверьте настройки интервала архивации

## 📚 Дополнительная документация

- [Полное руководство пользователя](./documentation/Руководство_пользователя_simple-SCADA.md)
- [Инструкция пользователя интерфейса](./documentation/Инструкция_пользователя_интерфейса.md)
- [README.md](./README.md)

## 🎯 Следующие шаги

После успешной настройки и запуска системы:

1. **Изучите интерфейс:** Ознакомьтесь с разделами "Конфигурация", "Реальное время" и "История"
2. **Настройте параметры опроса:** Оптимизируйте время ответа и период опроса для ваших устройств
3. **Изучите API:** Используйте REST API для программного управления системой
4. **Настройте интервал архивации:** Выберите оптимальный интервал для ваших задач

---

**Версия:** 1.0  
**Дата обновления:** 2026
