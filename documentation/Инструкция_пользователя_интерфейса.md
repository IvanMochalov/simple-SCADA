# ИНСТРУКЦИЯ ПОЛЬЗОВАТЕЛЯ ИНТЕРФЕЙСА

## Система диспетчерского управления и сбора данных simple-SCADA

**Версия:** 1.0  
**Дата:** 2026

---

## СОДЕРЖАНИЕ

1. [Введение](#введение)
2. [Обзор интерфейса](#обзор-интерфейса)
3. [Страница "Конфигурация"](#страница-конфигурация)
4. [Страница "Реальное время"](#страница-реальное-время)
5. [Страница "История"](#страница-история)
6. [Работа с системой в целом](#работа-с-системой-в-целом)
7. [Типичные сценарии работы](#типичные-сценарии-работы)

---

## ВВЕДЕНИЕ

Данная инструкция предназначена для пользователей, у которых система **simple-SCADA** уже установлена и запущена.
Инструкция описывает работу с веб-интерфейсом системы и основные операции.

**Перед началом работы убедитесь:**

- ✅ Сервер запущен и доступен
- ✅ Веб-интерфейс открыт в браузере
- ✅ Отображается индикатор подключения (зеленый)

**Адреса доступа:**

- Веб-интерфейс: `http://localhost:5173` (или IP адрес сервера)
- Сервер API: `http://localhost:3001`

---

## ОБЗОР ИНТЕРФЕЙСА

### Главное окно программы

Главное окно программы состоит из следующих элементов:

#### 1. Заголовок (Header)

**Расположение:** Верхняя часть экрана

**Элементы:**

- **Название системы:** "simple-SCADA"
- **Индикатор подключения:**
    - 🟢 Зеленый — подключение к серверу установлено
    - 🔴 Красный — нет подключения к серверу
- **Статус WebSocket:** отображается рядом с названием

#### 2. Навигационное меню

**Расположение:** Под заголовком

**Разделы:**

1. **Конфигурация** — управление узлами связи, устройствами и тегами
2. **Реальное время** — мониторинг текущих значений тегов
3. **История** — просмотр исторических данных

**Переключение между разделами:**

- Нажмите на название раздела в меню
- Активный раздел выделен цветом

#### 3. Область содержимого

**Расположение:** Основная часть экрана

**Содержимое зависит от выбранного раздела:**

- **Конфигурация:** древовидная структура системы
- **Реальное время:** карточки с текущими значениями тегов
- **История:** таблицы и графики исторических данных

### Индикаторы и статусы

#### Индикатор подключения к серверу

**Расположение:** В заголовке программы

**Статусы:**

- 🟢 **Подключено** — связь с сервером установлена, данные обновляются
- 🔴 **Отключено** — нет связи с сервером, данные не обновляются

**Что делать при отключении:**

1. Проверьте, что сервер запущен
2. Обновите страницу в браузере (F5)
3. Проверьте настройки сети и файрвола

#### Статусы узлов связи

**Расположение:** В разделе "Реальное время" и "Конфигурация"

**Статусы:**

- 🟢 **Подключен** — соединение с COM портом установлено, данные поступают
- ⚪ **Отключен** — соединение не установлено
- 🔴 **Ошибка соединения** — ошибка подключения (COM порт занят, устройство не отвечает)

---

## СТРАНИЦА "КОНФИГУРАЦИЯ"

### Назначение страницы

Страница **"Конфигурация"** предназначена для управления структурой системы:

- Создание, редактирование и удаление узлов связи
- Создание, редактирование и удаление устройств
- Создание, редактирование и удаление тегов
- Просмотр иерархической структуры системы
- Управление включением/выключением элементов

### Структура страницы

#### Заголовок страницы

**Элементы:**

- Название: "Конфигурация узлов связи"
- Кнопка **"Добавить узел связи"** — создание нового узла связи

#### Древовидная структура

**Иерархия:**

```
Узел связи (Connection Node)
  └─ Устройство (Device)
      └─ Тег (Tag)
```

**Особенности:**

- Все узлы и устройства развернуты по умолчанию
- Можно сворачивать/разворачивать узлы и устройства
- Клик по заголовку узла/устройства сворачивает/разворачивает его

### Работа с узлами связи

#### Просмотр узлов связи

**Отображение:**

- Каждый узел связи отображается в виде карточки
- В заголовке карточки отображается:
    - Название узла связи
    - COM порт (синий тег)
    - Статус включения (зеленый/серый тег)
    - Кнопки управления (редактировать, добавить устройство, удалить)

**Пример отображения:**

```
┌─────────────────────────────────────────────┐
│ RS-485 ТРМ212  [COM3]  [Включен]  [✏️][+][🗑️] │
└─────────────────────────────────────────────┘
```

#### Добавление узла связи

**Шаги:**

1. Нажмите кнопку **"Добавить узел связи"** в верхней части страницы
2. Заполните форму в открывшемся окне:

   **Обязательные поля:**
    - **Название** — введите название узла (например, "RS-485 линия 1")
    - **Тип узла** — выберите "COM порт"
    - **Порт** — введите COM порт (например, "COM3")
    - **Скорость (бод)** — выберите скорость (9600, 19200, 38400, 57600, 115200)

   **Дополнительные поля:**
    - **Биты данных** — выберите количество бит (5, 6, 7, 8)
    - **Стоп-биты** — выберите количество стоп-бит (1, 2)
    - **Четность** — выберите тип четности (не используется, четная, нечетная)
    - **Включен в работу** — переключатель (включен/выключен)

3. Нажмите кнопку **"Сохранить"**

**Результат:**

- Узел связи создан и отображается в списке
- Если узел включен в работу и Modbus Manager запущен, соединение инициализируется автоматически

#### Редактирование узла связи

**Шаги:**

1. Найдите нужный узел связи в списке
2. Нажмите кнопку **"✏️"** (редактировать) рядом с узлом связи
3. Измените параметры в открывшейся форме
4. Нажмите кнопку **"Сохранить"**

**Важно:**

- При изменении параметров COM порта система автоматически перезапускает Modbus соединение
- Если Modbus Manager не запущен, изменения сохранятся, но соединение не перезапустится

#### Удаление узла связи

**Шаги:**

1. Найдите нужный узел связи в списке
2. Нажмите кнопку **"🗑️"** (удалить) рядом с узлом связи
3. Подтвердите удаление в диалоговом окне:
    - Нажмите **"Удалить"** для подтверждения
    - Нажмите **"Отмена"** для отмены

**Важно:**

- При удалении узла связи удаляются все связанные устройства и теги
- Это действие нельзя отменить
- Все исторические данные для тегов этого узла также будут удалены

### Работа с устройствами

#### Просмотр устройств

**Отображение:**

- Устройства отображаются внутри узла связи
- В заголовке устройства отображается:
    - Название устройства
    - Статус включения (зеленый/серый тег)
    - Кнопки управления (редактировать, добавить тег, удалить)

**Пример отображения:**

```
┌─ RS-485 ТРМ212 (COM3)
  └─ ТРМ212-1  [Включен]  [✏️][+][🗑️]
```

#### Добавление устройства

**Шаги:**

1. Найдите нужный узел связи в списке
2. Нажмите кнопку **"+"** рядом с узлом связи
3. Заполните форму в открывшемся окне:

   **Обязательные поля:**
    - **Название** — введите название устройства (например, "ТРМ212-1")
    - **Адрес Modbus** — введите адрес устройства (0-255)
    - **Время ответа (мс)** — введите таймаут (минимум 100 мс)
    - **Период опроса (мс)** — введите интервал опроса (минимум 100 мс)
    - **Включен в работу** — переключатель (включен/выключен)

4. Нажмите кнопку **"Сохранить"**

**Результат:**

- Устройство создано и отображается внутри узла связи
- Если устройство включено в работу и Modbus Manager запущен, оно начнет опрашиваться автоматически

#### Редактирование устройства

**Шаги:**

1. Найдите нужное устройство в списке
2. Нажмите кнопку **"✏️"** (редактировать) рядом с устройством
3. Измените параметры в открывшейся форме
4. Нажмите кнопку **"Сохранить"**

**Важно:**

- При изменении параметров устройства система автоматически перезапускает Modbus соединение узла связи
- Изменения применяются только если Modbus Manager запущен

#### Удаление устройства

**Шаги:**

1. Найдите нужное устройство в списке
2. Нажмите кнопку **"🗑️"** (удалить) рядом с устройством
3. Подтвердите удаление в диалоговом окне

**Важно:**

- При удалении устройства удаляются все связанные теги
- Это действие нельзя отменить
- Все исторические данные для тегов этого устройства также будут удалены

### Работа с тегами

#### Просмотр тегов

**Отображение:**

- Теги отображаются внутри устройства
- Для каждого тега отображается:
    - Название тега
    - Статус включения (зеленый/серый тег)
    - Адрес регистра и тип регистра
    - Кнопки управления (редактировать, удалить)

**Пример отображения:**

```
┌─ ТРМ212-1
  └─ Температура датчика  [Вкл]  Адрес: 1 | HOLDING_REGISTER | ReadOnly  [✏️][🗑️]
```

#### Добавление тега

**Шаги:**

1. Найдите нужное устройство в списке
2. Нажмите кнопку **"+"** рядом с устройством
3. Заполните форму в открывшемся окне:

   **Обязательные поля:**
    - **Название** — введите название тега (например, "Температура датчика")
    - **Адрес регистра** — введите адрес регистра (0-65535)
    - **Тип регистра** — выберите тип (Holding Register, Input Register, Coil, Discrete Input)
    - **Тип данных в устройстве** — выберите тип (int16, int32, uint16, uint32, float)
    - **Тип данных в сервере** — выберите тип (int32, float и т.д.)
    - **Множитель** — выберите множитель (0.01, 0.1, 1.0, 10.0)
    - **Тип доступа** — выберите тип (ReadOnly, ReadWrite)
    - **Включен в работу** — переключатель (включен/выключен)

4. Нажмите кнопку **"Сохранить"**

**Результат:**

- Тег создан и отображается внутри устройства
- Если тег включен в работу и Modbus Manager запущен, он начнет опрашиваться автоматически

#### Редактирование тега

**Шаги:**

1. Найдите нужный тег в списке
2. Нажмите кнопку **"✏️"** (редактировать) рядом с тегом
3. Измените параметры в открывшейся форме
4. Нажмите кнопку **"Сохранить"**

**Важно:**

- При изменении параметров тега система автоматически перезапускает Modbus соединение узла связи
- Изменения применяются только если Modbus Manager запущен

#### Удаление тега

**Шаги:**

1. Найдите нужный тег в списке
2. Нажмите кнопку **"🗑️"** (удалить) рядом с тегом
3. Подтвердите удаление в диалоговом окне

**Важно:**

- При удалении тега удаляются все связанные исторические данные
- Это действие нельзя отменить

### Управление включением/выключением элементов

#### Включение/выключение узла связи

**Шаги:**

1. Откройте форму редактирования узла связи
2. Переключите переключатель **"Включен в работу"**
3. Нажмите **"Сохранить"**

**Результат:**

- Если узел включен и Modbus Manager запущен, соединение инициализируется
- Если узел выключен, соединение закрывается

#### Включение/выключение устройства

**Шаги:**

1. Откройте форму редактирования устройства
2. Переключите переключатель **"Включен в работу"**
3. Нажмите **"Сохранить"**

**Результат:**

- Если устройство включено и Modbus Manager запущен, оно начинает опрашиваться
- Если устройство выключено, опрос прекращается

#### Включение/выключение тега

**Шаги:**

1. Откройте форму редактирования тега
2. Переключите переключатель **"Включен в работу"**
3. Нажмите **"Сохранить"**

**Результат:**

- Если тег включен и Modbus Manager запущен, он начинает опрашиваться
- Если тег выключен, опрос прекращается

---

## СТРАНИЦА "РЕАЛЬНОЕ ВРЕМЯ"

### Назначение страницы

Страница **"Реальное время"** предназначена для:

- Мониторинга текущих значений всех тегов
- Запуска и остановки Modbus Manager
- Записи значений в теги с типом доступа ReadWrite
- Просмотра статусов подключения узлов связи
- Переподключения устройств при сбоях

### Структура страницы

#### Заголовок страницы

**Элементы:**

- Название: "Значения тегов в реальном времени"
- Кнопка **"Запустить Modbus Server"** / **"Остановить Modbus Server"** — управление Modbus Manager

#### Структура отображения данных

**Иерархия:**

```
Узел связи
  └─ Устройство
      └─ Теги (карточки с значениями)
```

**Особенности:**

- Отображаются только включенные узлы связи
- Все узлы и устройства развернуты по умолчанию
- Значения тегов обновляются автоматически через WebSocket

### Управление Modbus Manager

#### Запуск Modbus Manager

**Шаги:**

1. Откройте страницу **"Реальное время"**
2. Нажмите кнопку **"Запустить Modbus Server"** (зеленая кнопка)
3. Дождитесь инициализации соединений (кнопка изменится на "Остановить Modbus Server")

**Что происходит после запуска:**

- Система автоматически подключается ко всем включенным узлам связи
- Начинается периодический опрос всех включенных устройств
- Значения тегов начинают обновляться в реальном времени
- Автоматически начинается сбор исторических данных

**Индикация:**

- Кнопка меняется на **"Остановить Modbus Server"** (красная кнопка)
- Статусы узлов связи обновляются (подключен/отключен/ошибка)
- Значения тегов начинают отображаться

#### Остановка Modbus Manager

**Шаги:**

1. Нажмите кнопку **"Остановить Modbus Server"** (красная кнопка)
2. Дождитесь остановки (кнопка изменится на "Запустить Modbus Server")

**Что происходит после остановки:**

- Все Modbus соединения закрываются
- Опрос устройств прекращается
- Сбор исторических данных останавливается
- Значения тегов перестают обновляться

**Индикация:**

- Кнопка меняется на **"Запустить Modbus Server"** (зеленая кнопка)
- Статусы узлов связи меняются на "Отключен"
- Значения тегов показывают "Запустите Modbus Server"

### Просмотр данных в реальном времени

#### Структура отображения

**Уровень 1: Узел связи**

**Отображается:**

- Название узла связи
- COM порт (синий тег)
- Статус включения (зеленый/серый тег)
- Статус подключения (зеленый/красный тег, если Modbus Manager запущен)

**Пример:**

```
┌─ RS-485 ТРМ212 (COM3) [Включен в работу] [Соединение установлено]
```

**Уровень 2: Устройство**

**Отображается:**

- Название устройства
- Статус включения (зеленый/серый тег)
- Кнопка переподключения (🔄)

**Пример:**

```
└─ ТРМ212-1 [Включен в работу] [🔄]
```

**Уровень 3: Теги**

**Отображаются в виде карточек:**

- Название тега
- Текущее значение (крупным шрифтом)
- Временная метка последнего обновления
- Адрес регистра
- Статус включения

**Пример:**

```
└─ Температура датчика
   Адрес: 1
   25.60
   12:34:56
```

#### Статусы узлов связи

**Статусы:**

1. **🟢 Подключен** (зеленый тег)
    - Соединение с COM портом установлено
    - Данные поступают
    - Все устройства узла доступны

2. **⚪ Отключен** (серый тег)
    - Соединение не установлено
    - Modbus Manager не запущен или узел выключен

3. **🔴 Ошибка соединения** (красный тег)
    - Ошибка подключения к COM порту
    - Возможные причины:
        - COM порт занят другим приложением
        - COM порт не существует
        - Неверные настройки COM порта
    - При наведении отображается описание ошибки

#### Отображение значений тегов

**Формат отображения:**

- **Числовые значения:**
    - Целые числа отображаются без десятичной части
    - Дробные числа отображаются с двумя знаками после запятой
    - Значения отображаются крупным шрифтом (зеленым цветом)

- **Отсутствие данных:**
    - "—" — если значение не получено
    - "Нет данных" — если устройство не отвечает
    - "Запустите Modbus Server" — если Modbus Manager не запущен

- **Ошибки:**
    - Красным цветом отображается описание ошибки
    - Например: "TransactionTimedOutError"

- **Временная метка:**
    - Отображается время последнего обновления (формат: ЧЧ:ММ:СС)
    - Обновляется при каждом получении нового значения

### Запись значений в теги

#### Условия для записи

**Для записи значения необходимо:**

1. ✅ Тег должен иметь тип доступа **ReadWrite**
2. ✅ Modbus Manager должен быть запущен
3. ✅ Узел связи должен быть подключен
4. ✅ Устройство и тег должны быть включены в работу
5. ✅ Значение должно быть успешно прочитано (не null, без ошибок)

#### Процесс записи

**Шаги:**

1. Найдите нужный тег в разделе **"Реальное время"**
2. Убедитесь, что тег имеет тип доступа **ReadWrite** (значение будет подсвечено как редактируемое)
3. Нажмите на значение тега (оно станет редактируемым)
4. Введите новое значение:
    - Для целых чисел: введите целое число (например, 25)
    - Для дробных чисел: используйте точку или запятую (например, 25.5 или 25,5)
5. Нажмите **Enter** или кликните вне поля

**Что происходит:**

- Система проверяет корректность введенного значения
- Применяет обратное масштабирование (если указан множитель)
- Записывает значение в устройство
- Читает значение обратно для подтверждения
- Обновляет отображаемое значение

**Индикация:**

- При успешной записи появляется уведомление "Значение тега успешно записано"
- Значение обновляется на экране
- При ошибке появляется уведомление с описанием ошибки

#### Пример записи

**Исходные данные:**

- Тег: "Уставка температуры"
- Тип доступа: ReadWrite
- Текущее значение: 20.00
- Множитель: 0.1

**Действия:**

1. Нажимаем на значение "20.00"
2. Вводим "25.50"
3. Нажимаем Enter

**Результат:**

- Система записывает 255 в регистр (25.50 / 0.1 = 255)
- Система читает обратно и отображает 25.50
- Появляется уведомление об успешной записи

### Переподключение устройства

#### Когда использовать

**Переподключение устройства необходимо:**

- Если устройство перестало отвечать
- После изменения настроек устройства
- После сбоя связи
- Если значения тегов не обновляются

#### Процесс переподключения

**Шаги:**

1. Найдите нужное устройство в разделе **"Реальное время"**
2. Нажмите кнопку **"🔄"** (переподключить) рядом с устройством
3. Дождитесь завершения операции

**Что происходит:**

- Система перезагружает конфигурацию устройства
- Останавливает текущий опрос устройства
- Возобновляет опрос устройства с обновленной конфигурацией

**Индикация:**

- При успешном переподключении появляется уведомление "Устройство переподключено"
- Значения тегов начинают обновляться
- При ошибке появляется уведомление с описанием ошибки

---

## СТРАНИЦА "ИСТОРИЯ"

### Назначение страницы

Страница **"История"** предназначена для:

- Просмотра исторических данных в табличном виде
- Визуализации данных на графиках
- Фильтрации данных по уровню (система/узел/устройство)
- Фильтрации по временному диапазону
- Выбора тегов для отображения на графике

### Структура страницы

#### Заголовок страницы

**Элементы:**

- Название: "Исторические данные"
- Кнопка **"Обновить"** — обновление данных с текущими фильтрами
- Кнопка **"Фильтры"** — открытие панели фильтров

#### Панель фильтров

**Расположение:** Боковая панель (открывается по кнопке "Фильтры")

**Элементы:**

- Выбор уровня фильтрации (система/узел/устройство)
- Выбор узла связи (если уровень "Узел" или "Устройство")
- Выбор устройства (если уровень "Устройство")
- Выбор начальной даты и времени
- Выбор конечной даты и времени
- Кнопка **"Загрузить историю"**

#### Таблица исторических данных

**Расположение:** Основная область страницы

**Структура:**

- Колонка "Время" (фиксированная слева)
- Колонки тегов, сгруппированные по узлам связи → устройствам → тегам

#### График исторических данных

**Расположение:** Под таблицей

**Элементы:**

- Дерево выбора тегов для графика
- Интерактивный график с легендой

### Работа с фильтрами

#### Открытие панели фильтров

**Шаги:**

1. Нажмите кнопку **"Фильтры"** в верхней части страницы
2. Откроется боковая панель справа

#### Выбор уровня фильтрации

**Уровни:**

1. **Вся система**
    - Показывает данные всех узлов связи, устройств и тегов
    - Не требует дополнительных параметров

2. **Узел связи**
    - Показывает данные всех устройств выбранного узла связи
    - Требует выбора узла связи

3. **Устройство**
    - Показывает данные всех тегов выбранного устройства
    - Требует выбора узла связи и устройства

**Шаги:**

1. В панели фильтров выберите уровень из выпадающего списка **"Уровень"**
2. Если выбран уровень "Узел связи" или "Устройство":
    - Выберите узел связи из списка **"Узел связи"**
3. Если выбран уровень "Устройство":
    - Выберите устройство из списка **"Устройство"**

#### Выбор временного диапазона

**Шаги:**

1. В панели фильтров найдите раздел **"Начало"**
2. Выберите дату в календаре **"Дата"**
3. Выберите время в поле **"Время"** (формат: ЧЧ:ММ)
4. В разделе **"Конец"** выберите конечную дату и время

**Особенности:**

- Конечная дата не может быть раньше начальной
- Если начальная и конечная даты совпадают, конечное время не может быть раньше начального
- По умолчанию устанавливается период: последний час

**Пример:**

```
Начало: 01.01.2026 10:00
Конец: 01.01.2026 11:00
```

#### Загрузка данных

**Шаги:**

1. Настройте фильтры (уровень, узел/устройство, временной диапазон)
2. Нажмите кнопку **"Загрузить историю"** в панели фильтров
3. Дождитесь загрузки данных

**Результат:**

- Панель фильтров закрывается
- Таблица заполняется данными
- График обновляется с выбранными тегами
- Появляется уведомление об успешной загрузке

**Автоматическая загрузка:**

- При первом открытии страницы данные загружаются автоматически (последний час, вся система)
- Автоматическая загрузка происходит только один раз

### Работа с таблицей исторических данных

#### Структура таблицы

**Колонки:**

1. **Время** (фиксированная слева)
    - Временная метка записи
    - Формат: ДД.ММ.ГГГГ ЧЧ:ММ:СС
    - Можно сортировать по возрастанию/убыванию

2. **Колонки тегов** (сгруппированные)
    - Группировка: Узел связи → Устройство → Тег
    - Значения тегов в ячейках
    - Можно сортировать по возрастанию/убыванию

**Пример структуры:**

```
Время              | RS-485 ТРМ212
                   |   ТРМ212-1
                   |     Температура | Уставка
01.01.2026 10:00:00|     25.60      | 20.00
01.01.2026 10:01:00|     25.65      | 20.00
```

#### Операции с таблицей

**Сортировка:**

1. Нажмите на заголовок колонки для сортировки
2. Повторное нажатие меняет направление сортировки (по возрастанию/убыванию)
3. По умолчанию таблица отсортирована по времени (по убыванию)

**Пагинация:**

1. В нижней части таблицы найдите элементы управления пагинацией
2. Выберите размер страницы: 10, 20, 50, 100, 200 записей
3. Используйте кнопки навигации для перехода между страницами

**Прокрутка:**

- **Горизонтальная:** для просмотра всех колонок тегов
- **Вертикальная:** для просмотра всех записей (высота таблицы: 600px)

#### Форматирование значений

**Числовые значения:**

- Целые числа отображаются без десятичной части
- Дробные числа отображаются с двумя знаками после запятой
- Значения выделены синим цветом

**Отсутствие данных:**

- "—" — если значение отсутствует для данного времени

### Работа с графиком исторических данных

#### Выбор тегов для графика

**Шаги:**

1. В разделе **"График исторических данных"** найдите дерево выбора тегов
2. Используйте дерево для выбора тегов:
    - Структура: **Узел связи → Устройство → Тег**
    - Отметьте нужные теги галочками
    - Снимите галочки с ненужных тегов
3. График автоматически обновится с выбранными тегами

**Особенности:**

- По умолчанию выбраны все теги
- Можно выбрать несколько тегов одновременно
- Можно выбрать все теги узла или устройства (отметить родительский элемент)

#### Просмотр графика

**Элементы графика:**

1. **Ось X (время)**
    - Временная шкала
    - Подписи под углом для удобства чтения
    - Автоматическое масштабирование

2. **Ось Y (значения)**
    - Шкала значений
    - Автоматическое масштабирование под диапазон данных

3. **Линии данных**
    - Каждый тег отображается линией своего цвета
    - Точки данных на линии
    - Поддержка нескольких линий одновременно

4. **Легенда**
    - Названия всех отображаемых тегов
    - Цвета линий
    - Можно скрыть/показать линию, кликнув на название в легенде

#### Интерактивные возможности

**Наведение на точку:**

- При наведении на точку графика отображается:
    - Точное значение тега
    - Время записи
    - Название тега

**Масштабирование:**

- Можно масштабировать график, используя колесико мыши
- Можно перемещать график, перетаскивая мышью

**Форматирование значений в подсказке:**

- Целые числа отображаются без десятичной части
- Дробные числа отображаются с двумя знаками после запятой

#### Обновление данных

**Автоматическое обновление:**

- График обновляется при изменении выбранных тегов
- График обновляется при загрузке новых данных

**Ручное обновление:**

1. Нажмите кнопку **"Обновить"** в верхней части страницы
2. Данные перезагрузятся с текущими фильтрами
3. График обновится автоматически

---

## РАБОТА С СИСТЕМОЙ В ЦЕЛОМ

### Навигация по системе

#### Переключение между разделами

**Способы:**

1. **Через меню:**
    - Нажмите на название раздела в навигационном меню
    - Активный раздел выделен цветом

2. **Прямой переход:**
    - Используйте закладки браузера для быстрого доступа
    - Сохраните ссылки на нужные разделы

#### Сохранение состояния

**Особенности:**

- Состояние фильтров в разделе "История" не сохраняется при перезагрузке страницы
- Состояние развернутых/свернутых узлов и устройств не сохраняется
- Конфигурация системы сохраняется в базе данных

### Типичные операции

#### Полный цикл работы с системой

**1. Настройка конфигурации (раздел "Конфигурация"):**

- Создание узла связи
- Создание устройств
- Создание тегов
- Включение элементов в работу

**2. Запуск системы (раздел "Реальное время"):**

- Запуск Modbus Manager
- Проверка подключения узлов связи
- Проверка получения данных

**3. Мониторинг данных (раздел "Реальное время"):**

- Просмотр текущих значений тегов
- Запись значений в теги (при необходимости)
- Переподключение устройств (при сбоях)

**4. Анализ истории (раздел "История"):**

- Настройка фильтров
- Просмотр данных в таблице
- Анализ данных на графике

### Уведомления системы

#### Типы уведомлений

**1. Успешные операции (зеленые):**

- "Узел связи создан"
- "Устройство создано"
- "Тег создан"
- "Значение тега успешно записано"
- "Устройство переподключено"
- "История успешно загружена"

**2. Предупреждения (желтые):**

- "Modbus Manager остановлен"
- "Нет включенных узлов связи"

**3. Ошибки (красные):**

- "Ошибка при создании узла связи"
- "Ошибка при записи значения"
- "Ошибка загрузки истории"
- Описания ошибок Modbus

#### Работа с уведомлениями

**Отображение:**

- Уведомления появляются в правом верхнем углу экрана
- Автоматически исчезают через несколько секунд
- Можно закрыть вручную, нажав на крестик

**Действия при ошибках:**

1. Прочитайте описание ошибки
2. Следуйте рекомендациям в разделе "Устранение типовых проблем"
3. При необходимости обратитесь к документации

### Проверка состояния системы

#### Быстрая проверка

**Индикаторы для проверки:**

1. **Индикатор подключения к серверу** (в заголовке):
    - 🟢 Зеленый — все в порядке
    - 🔴 Красный — нет связи с сервером

2. **Статус Modbus Manager** (раздел "Реальное время"):
    - Кнопка "Остановить Modbus Server" — запущен
    - Кнопка "Запустить Modbus Server" — остановлен

3. **Статусы узлов связи** (раздел "Реальное время"):
    - 🟢 Подключен — все в порядке
    - 🔴 Ошибка соединения — требуется проверка

4. **Значения тегов:**
    - Отображаются значения — все в порядке
    - "Нет данных" — требуется проверка

#### Полная проверка

**Последовательность:**

1. Проверьте индикатор подключения к серверу
2. Откройте раздел "Реальное время"
3. Проверьте статус Modbus Manager (должен быть запущен)
4. Проверьте статусы всех узлов связи (должны быть "Подключен")
5. Проверьте значения тегов (должны обновляться)
6. При необходимости используйте кнопку "Переподключить" для устройств

---

## ТИПИЧНЫЕ СЦЕНАРИИ РАБОТЫ

### Сценарий 1: Первый запуск системы

**Задача:** Настроить систему с нуля и начать мониторинг.

**Шаги:**

1. **Настройка конфигурации:**
    - Откройте раздел "Конфигурация"
    - Создайте узел связи (укажите COM порт и настройки)
    - Создайте устройство (укажите адрес Modbus)
    - Создайте теги (укажите адреса регистров)
    - Убедитесь, что все элементы включены в работу

2. **Запуск системы:**
    - Откройте раздел "Реальное время"
    - Нажмите "Запустить Modbus Server"
    - Дождитесь подключения узлов связи

3. **Проверка работы:**
    - Проверьте статусы узлов связи (должны быть "Подключен")
    - Проверьте значения тегов (должны обновляться)
    - Проверьте временные метки (должны обновляться)

4. **Мониторинг:**
    - Наблюдайте за значениями тегов в реальном времени
    - При необходимости записывайте значения в теги ReadWrite

### Сценарий 2: Ежедневная работа

**Задача:** Ежедневный мониторинг и управление системой.

**Шаги:**

1. **Проверка состояния:**
    - Откройте раздел "Реальное время"
    - Проверьте индикатор подключения к серверу
    - Проверьте статус Modbus Manager (должен быть запущен)
    - Проверьте статусы узлов связи

2. **Мониторинг данных:**
    - Просматривайте текущие значения тегов
    - Обращайте внимание на изменения значений
    - Проверяйте временные метки (должны обновляться)

3. **Управление (при необходимости):**
    - Записывайте значения в теги ReadWrite
    - Переподключайте устройства при сбоях

4. **Анализ истории:**
    - Откройте раздел "История"
    - Настройте фильтры (выберите период)
    - Просмотрите данные в таблице
    - Проанализируйте данные на графике

### Сценарий 3: Добавление нового устройства

**Задача:** Добавить новое устройство в существующую систему.

**Шаги:**

1. **Подготовка:**
    - Убедитесь, что устройство физически подключено
    - Узнайте адрес Modbus устройства
    - Узнайте адреса регистров для тегов

2. **Создание устройства:**
    - Откройте раздел "Конфигурация"
    - Найдите нужный узел связи
    - Нажмите "+" рядом с узлом связи
    - Заполните форму создания устройства
    - Нажмите "Сохранить"

3. **Создание тегов:**
    - Нажмите "+" рядом с созданным устройством
    - Заполните форму создания тега
    - Повторите для всех необходимых тегов

4. **Проверка:**
    - Откройте раздел "Реальное время"
    - Убедитесь, что Modbus Manager запущен
    - Проверьте, что значения тегов обновляются

### Сценарий 4: Устранение сбоя

**Задача:** Восстановить работу системы после сбоя.

**Шаги:**

1. **Диагностика:**
    - Проверьте индикатор подключения к серверу
    - Откройте раздел "Реальное время"
    - Проверьте статусы узлов связи
    - Определите проблемный элемент

2. **Устранение проблемы:**
    - Если узел связи с ошибкой:
        - Проверьте COM порт (не занят ли другим приложением)
        - Проверьте настройки COM порта
        - Перезапустите Modbus Manager
    - Если устройство не отвечает:
        - Используйте кнопку "Переподключить"
        - Проверьте физическое подключение
        - Проверьте адрес Modbus

3. **Проверка восстановления:**
    - Проверьте статусы узлов связи (должны быть "Подключен")
    - Проверьте значения тегов (должны обновляться)
    - Проверьте временные метки (должны обновляться)

### Сценарий 5: Анализ исторических данных

**Задача:** Проанализировать изменения параметров за определенный период.

**Шаги:**

1. **Настройка фильтров:**
    - Откройте раздел "История"
    - Нажмите "Фильтры"
    - Выберите уровень фильтрации (система/узел/устройство)
    - Выберите временной диапазон
    - Нажмите "Загрузить историю"

2. **Просмотр в таблице:**
    - Просмотрите данные в таблице
    - Используйте сортировку для поиска экстремальных значений
    - Используйте пагинацию для просмотра всех данных

3. **Анализ на графике:**
    - Выберите теги для отображения на графике
    - Проанализируйте тренды изменений
    - Используйте интерактивные возможности графика

4. **Выводы:**
    - Определите закономерности изменений
    - Выявите аномалии
    - При необходимости экспортируйте данные (через API)

---

## ЗАКЛЮЧЕНИЕ

Данная инструкция описывает работу с веб-интерфейсом системы **simple-SCADA**. Основные моменты:

- ✅ Система состоит из трех основных разделов: Конфигурация, Реальное время, История
- ✅ Все операции выполняются через веб-интерфейс
- ✅ Данные обновляются автоматически через WebSocket
- ✅ Система предоставляет интуитивно понятный интерфейс для управления

**Для получения дополнительной информации:**

- Обратитесь к полному руководству пользователя
- Изучите раздел "Устранение типовых проблем"
- Ознакомьтесь с FAQ

---

**Версия документа:** 1.0  
**Дата обновления:** 2026
