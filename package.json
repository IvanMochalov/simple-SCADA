{
  "name": "diplom2",
  "version": "1.0.0",
  "description": "Система диспетчерского управления и сбора данных для технологических объектов управления",
  "private": true,
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "build": "cd client && npm run build",
    "start": "npm run build && cd server && cross-env NODE_ENV=production npm start",
    "install:all": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "cross-env": "^7.0.3"
  },
  "dependencies": {
    "@ant-design/icons": "^6.1.0"
  }
}
